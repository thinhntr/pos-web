/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['vi'],
        defaultLocale: 'vi'
    },
    reactStrictMode: true,
    swcMinify: true
};

module.exports = nextConfig;
